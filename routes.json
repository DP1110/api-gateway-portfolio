{
    "routes": [
        {
            "prefix": "/users",
            "strip_prefix": false,
            "backends": [
                {"url": "http://users-service:9001", "weight": 1}
            ],
            "description": "Users microservice"
        },
        {
            "prefix": "/orders",
            "strip_prefix": false,
            "backends": [
                {"url": "http://orders-service:9002", "weight": 1}
            ],
            "description": "Orders microservice"
        }
    ],

    "_comment": "Edit this file while the gateway is running — changes are picked up automatically (hot-reload)."
}
